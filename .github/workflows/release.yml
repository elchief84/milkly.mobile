name: Release Build

on:
  push:
    tags:
      - 'v*.*.*'  # Triggers on version tags like v1.0.0
  workflow_dispatch:  # Allows manual trigger

jobs:
  build-android-release:
    name: Build Android Release
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'
          channel: 'stable'
          cache: true
      
      - name: Get dependencies
        run: flutter pub get
      
      - name: Build APK (Release)
        run: flutter build apk --release
      
      - name: Build App Bundle (Release)
        run: flutter build appbundle --release
      
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release-apk
          path: build/app/outputs/flutter-apk/app-release.apk
          retention-days: 30
      
      - name: Upload App Bundle
        uses: actions/upload-artifact@v4
        with:
          name: app-release-bundle
          path: build/app/outputs/bundle/release/app-release.aab
          retention-days: 30
      
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            build/app/outputs/flutter-apk/app-release.apk
            build/app/outputs/bundle/release/app-release.aab
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # iOS build temporarily disabled - uncomment when self-hosted runner is ready
  # build-ios-release:
  #   name: Build iOS Release
  #   runs-on: self-hosted  # Self-hosted runner on your Mac
  #   timeout-minutes: 45
  #   
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #     
  #     - name: Setup Flutter
  #       uses: subosito/flutter-action@v2
  #       with:
  #         flutter-version: '3.24.0'
  #         channel: 'stable'
  #         cache: true
  #         architecture: arm64
  #     
  #     - name: Install CocoaPods
  #       run: |
  #         cd ios
  #         pod install
  #     
  #     - name: Get dependencies
  #       run: flutter pub get
  #     
  #     # Note: For actual App Store deployment, you'll need:
  #     # 1. Apple Developer certificates in GitHub Secrets
  #     # 2. Provisioning profiles
  #     # 3. App Store Connect API key
  #     # See: https://docs.github.com/en/actions/deployment/deploying-to-your-cloud-provider/deploying-to-apple-app-store
  #     
  #     - name: Build iOS (no codesign)
  #       run: flutter build ios --release --no-codesign
  #     
  #     - name: Create IPA (unsigned)
  #       run: |
  #         mkdir -p Payload
  #         cp -r build/ios/iphoneos/Runner.app Payload/
  #         zip -r milkly-unsigned.ipa Payload
  #         rm -rf Payload
  #     
  #     - name: Upload IPA
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: ios-release-ipa
  #         path: milkly-unsigned.ipa
  #         retention-days: 30

